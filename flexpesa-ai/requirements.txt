# Minimal Investment Portfolio API Requirements
# Fast startup, lightweight, production-ready

# Core FastAPI dependencies (same as before)
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
sqlalchemy==2.0.23
pydantic-settings==2.1.0

# Essential data analysis (lightweight)
pandas==2.1.4
numpy==1.24.3

# Market data (already included)
yfinance==0.2.28

# Async HTTP for news APIs (lightweight)
aiohttp==3.9.1

# HTTP requests (already used)
requests==2.31.0

# Optional: News API client (lightweight)
newsapi-python==0.2.7

# Development/testing (optional)
pytest==7.4.3
pytest-asyncio==0.21.1